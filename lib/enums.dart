

enum EnvironmentType{
  test,
  live
}

enum ColorType{
  danger,
  error,
  info,
  infoLight,
  faint,
  light,
  dark,
  warning,
  secondary,
  secondary2,
  divergent,
  divergentLight,
  outlier,
  successLight,
  success
}

enum AppointmentChatTab{
  appointment,
  chat
}

enum ChatOwner{
  me,
  doctor
}

enum AppointmentBookingState{
  enterAppointmentNote,
  pickAppointmentDate,
  idle
}

